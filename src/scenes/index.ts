import { MainMenuScene } from './main-menu-scene';
import { BootScene } from './boot-scene';
import { GameScene } from './game-scene';
import { MapEditorScene } from './map-editor-scene';

export default [
  BootScene,
  MainMenuScene,
  GameScene,
  MapEditorScene,
];
